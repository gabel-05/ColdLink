# Optional: enable Google Maps JavaScript API on the Network Map page (markers + static route line).
# Create a browser key with Maps JavaScript API enabled and restrict by HTTP referrer.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
